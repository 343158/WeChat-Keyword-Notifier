#!/bin/bash
 # Source: # Copyright 2015 the original author or authors. # DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER. # # This file is part of the Gradle build system. #  # Attempt to set APP_HOME # Resolve links: $0 may be a link PRG="$0" # Need this for relative symlinks. while [ -h "$PRG" ] ; do     ls=`ls -ld "$PRG"`     link=`expr "$ls" : '.*-> \(.*\)$'`     if expr "$link" : '/.*' > /dev/null; then         PRG="$link"     else         PRG=`dirname "$PRG"`"/$link"     fi done SAVED=`pwd` cd "`dirname \"$PRG\"`/" >/dev/null APP_HOME=`pwd -P` cd "$SAVED" >/dev/null  # Add default JVM options here. You can also use JAVA_OPTS and GRADLE_OPTS to pass JVM options to this script. DEFAULT_JVM_OPTS="-Xmx64m -Xms64m"  # Use the maximum available, or set MAX_FD != -1 to use that value. MAX_FD="maximum"  warn () {     echo "$*" }  die () {     echo     echo "$*"     echo     exit 1 }  # OS specific support (must be 'true' or 'false'). cygwin=false msys=false darwin=false nonstop=false case "`uname`" in   CYGWIN* )     cygwin=true     ;;   Darwin* )     darwin=true     ;;   MINGW* )     msys=true     ;; esac  # Attempt to set APP_HOME # Resolve links: $0 may be a link PRG="$0" # Need this for relative symlinks. while [ -h "$PRG" ] ; do     ls=`ls -ld "$PRG"`     link=`expr "$ls" : '.*-> \(.*\)$'`     if expr "$link" : '/.*' > /dev/null; then         PRG="$link"     else         PRG=`dirname "$PRG"`"/$link"     fi done SAVED=`pwd` cd "`dirname \"$PRG\"`/" >/dev/null APP_HOME=`pwd -P` cd "$SAVED" >/dev/null  # Add default JVM options here. You can also use JAVA_OPTS and GRADLE_OPTS to pass JVM options to this script. DEFAULT_JVM_OPTS="-Xmx64m -Xms64m"  # Use the maximum available, or set MAX_FD != -1 to use that value. MAX_FD="maximum"  CLASSPATH=$APP_HOME/gradle/wrapper/gradle-wrapper.jar  # Determine the Java command to use with their workspaces. if [ -n "$JAVA_HOME" ] ; then     if [ -x "$JAVA_HOME/jre/sh/java" ] ; then         # IBM's JDK on AIX uses strange locations for the executables         JAVACMD="$JAVA_HOME/jre/sh/java"     else         JAVACMD="$JAVA_HOME/bin/java"     fi     if [ ! -x "$JAVACMD" ] ; then         die "ERROR: JAVA_HOME is set to an invalid directory: $JAVA_HOME  Please set the JAVA_HOME variable in your environment to match the location of your Java installation."     fi else     JAVACMD="java"     which java >/dev/null 2>&1 || die "ERROR: JAVA_HOME is not set and no 'java' command could be found in your PATH.  Please set the JAVA_HOME variable in your environment to match the location of your Java installation, or install a JDK that contains Java." fi  # Increase the maximum file descriptors if we can. if [ "$cygwin" = "false" -a "$darwin" = "false" -a "$nonstop" = "false" ] ; then     MAX_FD_LIMIT=`ulimit -H -n`     if [ $? -eq 0 ] ; then         if [ "$MAX_FD" = "maximum" -o "$MAX_FD" = "maximum" ] ; then             MAX_FD="$MAX_FD_LIMIT"         fi         ulimit -n $MAX_FD         if [ $? -ne 0 ] ; then             warn "Could not set maximum file descriptor limit: $MAX_FD"         fi     else         warn "Could not query maximum file descriptor limit: $MAX_FD_LIMIT"     fi fi  # For Darwin, add options to specify how the application appears in the dock if $darwin; then     GRADLE_OPTS="$GRADLE_OPTS -Dapple.awt.application.appearance=system" fi  # For Cygwin, switch paths to Windows format before running java if $cygwin ; then     APP_HOME=`cygpath --path --windows "$APP_HOME"`     CLASSPATH=`cygpath --path --windows "$CLASSPATH"`     JAVACMD=`cygpath --path --windows "$JAVACMD"`      # We build the pattern for arguments to be converted to Windows format by cygpath     ROOTDIRSRAW=`find -L / -maxdepth 1 -mindepth 1 -type d 2>/dev/null`     SEP=""     for dir in $ROOTDIRSRAW ; do         ROOTDIRS="$ROOTDIRS$SEP$dir"         SEP="|"     done     OURCYGPATTERN="(^($ROOTDIRS))"     # Add a user-defined pattern to the cygpath arguments     if [ "$GRADLE_CYGPATTERN" != "" ] ; then         OURCYGPATTERN="$OURCYGPATTERN|($GRADLE_CYGPATTERN)"     fi     # Now convert the arguments - kludge to limit ourselves to /bin/sh     i=0     for arg in "$@" ; do         CHECK=`echo "$arg"|grep -E "$OURCYGPATTERN" -`         if [ "$CHECK" ] ; then             arg=`cygpath --path --ignore --mixed "$arg"`         fi         eval "arg$i=\"$arg\""         i=$((i+1))     done     case $i in         (0) ;;         (1) set -- "$arg0" ;;         (2) set -- "$arg0" "$arg1" ;;         (3) set -- "$arg0" "$arg1" "$arg2" ;;         (4) set -- "$arg0" "$arg1" "$arg2" "$arg3" ;;         (5) set -- "$arg0" "$arg1" "$arg2" "$arg3" "$arg4" ;;         (6) set -- "$arg0" "$arg1" "$arg2" "$arg3" "$arg4" "$arg5" ;;         (7) set -- "$arg0" "$arg1" "$arg2" "$arg3" "$arg4" "$arg5" "$arg6" ;;         (8) set -- "$arg0" "$arg1" "$arg2" "$arg3" "$arg4" "$arg5" "$arg6" "$arg7" ;;         (9) set -- "$arg0" "$arg1" "$arg2" "$arg3" "$arg4" "$arg5" "$arg6" "$arg7" "$arg8" ;;     esac fi  # Split up the JVM_OPTS And GRADLE_OPTS values into an array, then the elements can be passed as arguments to the java command function splitJvmOpts() {     JVM_OPTS=("$@") } eval "splitJvmOpts $DEFAULT_JVM_OPTS $JAVA_OPTS $GRADLE_OPTS" JVM_OPTS[${#JVM_OPTS[*]}]="-Dorg.gradle.appname=$(basename \"$0\")"  exec "$JAVACMD" "${JVM_OPTS[@]}" -classpath "$CLASSPATH" org.gradle.wrapper.GradleWrapperMain "$@" 